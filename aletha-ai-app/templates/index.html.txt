// Ganti bagian tanyaAI() di script HTML kamu jadi begini:
async function tanyaAI() {
    // ... (kode ambil input dan tampilkan chat user sama seperti tadi)

    try {
        const response = await fetch('/ask', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ message: text }) // Kirim pesan ke Python
        });

        const data = await response.json();
        
        if (data.response) {
            // Tampilkan data.response ke chatbox
        }
    } catch (error) {
        // Tampilkan error
    }
}